{
  "buildCommand": "npm install && cd admin && npm install && npm run build",
  "rewrites": [
    {
      "source": "/admin/(.*)",
      "destination": "/admin/dist/index.html"
    },
    {
      "source": "/admin",
      "destination": "/admin/dist/index.html"
    }
  ]
}
